// Scrollable stories section
$(document).ready(function() {
    $(".stories-section").scroll(function() {
        let scrollLeft = $(this).scrollLeft();
        // Add any scroll effect if needed
    });
});
